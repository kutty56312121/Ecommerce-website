# Client (User)

- React + Vite + Tailwind
- Redux Toolkit for state (auth, products, cart)

Env:
- copy `.env.example` to `.env`

Scripts:
- dev: `npm run dev`
- build: `npm run build`
- preview: `npm run preview`
